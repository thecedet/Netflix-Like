import { TableSetColorDirective } from './table-set-color.directive';

describe('TableSetColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TableSetColorDirective();
    expect(directive).toBeTruthy();
  });
});
